Let's take this to the next level. The floating cards concept is great, but there aren't enough of them, and the initial Hero Section isn't impactful enough yet. We need it to look extremely high-end and perform flawlessly on mobile browsers.

Please update the code with the following improvements:

1. Hero Section Overhaul (The "Wow" Entry):
   - Redesign the Hero section (`100dvh`). Add a subtle, slow-moving animated gradient mesh or glowing orbs in the background specifically for this section.
   - Typography: Use CSS `clamp()` for the main headline so it scales perfectly from massive desktop screens to small mobile screens. 
   - Entry Animation: Use GSAP to create a stunning entry animation for the headline (e.g., splitting the text into characters or lines, and revealing them with a stagger, sliding up from invisible masks).
   - Do NOT add any "scroll down" buttons or indicators. Keep the Hero section completely clean and minimalist.

2. Expand the Floating Gallery (More Density & Depth):
   - Increase the number of floating website mockup cards to 20-30. 
   - Spread them out over a much larger virtual 3D space (wider X-axis and deeper Z-axis) so the background feels dense and infinite as the user scrolls. 
   - Ensure the GSAP parallax logic uses `translate3d` to force hardware acceleration, keeping the framerate high.

3. Premium Aesthetic Touch (Grain Overlay):
   - Add a fixed, pointer-events-none layer over the entire site with a subtle CSS noise/grain texture. This gives the site a premium, cinematic, tactile feel often seen on Awwwards sites.

4. Strict Mobile Optimization:
   - On mobile screens (max-width: 768px), scale down the cards appropriately so they don't overwhelm the screen.
   - Ensure absolutely no horizontal scrolling (`overflow-x: hidden` on the body).
   - Keep the glassmorphism effects on the foreground text blocks, but ensure the contrast is high enough so the text is perfectly readable over the busy moving background.